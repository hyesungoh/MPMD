import React from "react";
import Actor from "../Actor";

const ActorCard = (props) => {
    console.log(props);
    return (
        <div>
            <h1>card</h1>
        </div>
    );
};

export default ActorCard;
